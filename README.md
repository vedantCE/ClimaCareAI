# Project Setup Guide

Follow the steps below to run the project (Backend + Frontend).

---

## 🚀 Step 1: Navigate to Backend

```bash
cd backend
```

---

## 🖥️ Step 2: Backend Setup

### **For Windows**

```bash
python -m venv venv
venv\Scripts\activate     # CMD or PowerShell
# OR
source venv/Scripts/activate   # Git Bash

pip install -r requirements.txt
python main.py
```

### **For macOS / Linux**

```bash
python3 -m venv venv
source venv/bin/activate

pip install -r requirements.txt
python3 main.py
```

---

## 🧩 Step 3: Frontend Setup

Open a **new terminal**:

```bash
npm install
npm run dev
```

Your project should now be running successfully! 🎉
